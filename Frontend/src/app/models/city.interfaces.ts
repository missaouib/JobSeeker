import {ID} from '@datorama/akita';

export interface City {
  id: ID,
  name: string,
  population: number,
  area: number,
  density: number,
  linkedin: number,
  pracuj: number,
  noFluffJobs: number,
  justJoin: number,
  total: number,
  per100k: number
}
