import { ID } from '@datorama/akita';

export interface Technology {
    id: ID,
    name: string,
    type: string,
    linkedin: number,
    pracuj: number,
    noFluffJobs: number,
    justJoin: number,
    total: number
}
